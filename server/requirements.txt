Django>=6.1,<6.2
djangorestframework>=3.18.0,<4
django-allauth>=65.18,<66
psycopg[binary]>=3.3.4,<4
requests>=2.34.2,<3
whitenoise>=6.12.0,<7
gunicorn>=23,<24
# Only imported when DJANGO_REDIS_URL is set (see CACHES in config/settings.py);
# Django's built-in RedisCache backend needs the client library present.
redis>=6.0,<7
